# SnapImg ユーザーマニュアル

## 1. SnapImg について

SnapImg は、画像ファイルを JPG、PNG、WEBP、BMP などへ変換する Windows 用アプリです。

複数の画像をまとめて変換できるほか、画像サイズの変更、Exif 情報の保持、保存先の指定にも対応しています。

## 2. インストール

1. 配布された `SnapImg-Setup-*.exe` を実行します。
2. 画面の指示に従ってインストールします。
3. インストール完了後、スタートメニューまたはデスクトップの「SnapImg」から起動します。

アンインストールする場合は、Windows の「設定」→「アプリ」→「インストールされているアプリ」から SnapImg を削除してください。

## 3. 対応形式

### 読み込み

- HEIC / HEIF
- JPG / JPEG
- PNG
- WEBP
- BMP
- TIFF / TIF
- GIF
- ICO

### 出力

- JPG
- PNG
- WEBP
- BMP

## 4. 基本的な使い方

1. SnapImg を起動します。
2. 「ファイルを追加」を押して画像を選択します。
3. または、画像ファイルやフォルダをファイル一覧へドラッグ＆ドロップします。
4. 右側の「変換設定」で出力形式などを指定します。
5. 「変換を開始」を押します。

変換後のファイルは、初期設定では元画像と同じフォルダに保存されます。

## 5. ファイルの追加と削除

### ファイルを追加

「ファイルを追加」を押し、1 つ以上の画像を選択します。

### フォルダを追加

「フォルダを追加」を押すと、フォルダ内およびサブフォルダ内の対応画像をまとめて追加できます。

### ドラッグ＆ドロップ

画像ファイルまたはフォルダを、SnapImg のファイル一覧へドロップします。

### 選択を削除

ファイル一覧で削除したい項目を選択し、「選択を削除」を押します。元の画像ファイルは削除されません。

### リストをクリア

「リストをクリア」を押すと、一覧からすべての項目を削除します。元の画像ファイルは削除されません。

## 6. 変換設定

### 出力形式

変換後の形式を `jpg`、`png`、`webp`、`bmp` から選択します。

### 画質

JPEG と WEBP の画質を 10～100 の範囲で設定できます。

数値が大きいほど画質は高くなりますが、ファイルサイズも大きくなります。通常は 80～90 程度がおすすめです。

PNG と BMP では画質設定は使用されません。

### Exif 情報を保持する

チェックを入れると、撮影日時やカメラ情報などの Exif 情報を可能な範囲で保持します。

個人情報や撮影場所情報を削除したい場合は、チェックを外してください。

### リサイズ

幅と高さをピクセル単位で入力します。

- 幅だけ入力：幅を基準に高さを自動計算
- 高さだけ入力：高さを基準に幅を自動計算
- 両方入力：指定した範囲に収まるように変換
- 「アスペクト比を維持」を外す：幅と高さを指定値に変更

空欄の場合、元のサイズを維持します。

### 保存先

- 「元ファイルと同じフォルダ」：元画像と同じ場所に保存
- 「指定フォルダに保存」：選択したフォルダに保存

同名のファイルがすでに存在する場合は、`_1`、`_2` のような連番が自動的に付加されます。既存ファイルは上書きされません。

## 7. HEIC を JPG に変換する

1. 「ファイルを追加」で HEIC ファイルを選択します。
2. 出力形式で `jpg` を選択します。
3. 必要に応じて画質、Exif、サイズ、保存先を設定します。
4. 「変換を開始」を押します。

大量の HEIC を変換する場合は、フォルダごと追加すると便利です。

## 8. エクスプローラーの右クリックメニュー

ファイルを選択して、直接 JPG などへ変換できます。

### 登録方法

1. SnapImg を起動します。
2. 画面右上の「設定」を押します。
3. 「はい」を選択します。
4. 登録したい形式ごとに「はい」を選択します。

### 使用方法

エクスプローラーで画像ファイルを右クリックし、「JPGに変換」「PNGに変換」などを選択します。

Windows 11 では、右クリックメニューの「その他のオプションを確認」の中に表示される場合があります。

### 解除方法

1. SnapImg の「設定」を押します。
2. 最初の確認で「いいえ」を選択します。

登録済みの右クリックメニューが解除されます。

## 9. コマンドライン操作

PowerShell またはコマンドプロンプトから変換できます。

```powershell
snapimg.exe --convert-to jpg "C:\Images\photo.heic"
```

フォルダを指定すると、フォルダ内の対応画像をまとめて変換します。

```powershell
snapimg.exe --convert-to png "C:\Images"
```

右クリックメニューの登録・解除：

```powershell
snapimg.exe --register-menu
snapimg.exe --unregister-menu
```

## 10. 設定ファイル

ウィンドウサイズは次の場所に保存されます。

```text
C:\Users\<ユーザー名>\AppData\Local\imgconv\settings.json
```

設定ファイルを削除すると、次回起動時に初期サイズへ戻ります。

## 11. トラブルシューティング

### アプリが起動しない

- インストーラーで再インストールしてください。
- インストール先のファイルを一部だけ移動・削除していないか確認してください。
- 配布版は `snapimg.exe` だけでなく、インストールされたファイル一式が必要です。

### 変換に失敗する

- 元画像が破損していないか確認してください。
- 保存先に書き込み権限があるか確認してください。
- 同じ画像を画像ビューアなどで開いている場合は閉じてください。
- ファイル名やフォルダ名に特殊な文字が含まれる場合は、別の場所へコピーして試してください。

### HEIC の変換に失敗する

- Windows の「HEIF 画像拡張機能」をインストールしてください。
- iPhone などから再度コピーした元ファイルで試してください。
- まず 1 ファイルだけで変換できるか確認してください。

### 変換後の画像が見つからない

初期設定では元画像と同じフォルダに保存されます。設定で「指定フォルダに保存」を選択した場合は、そのフォルダを確認してください。

## 12. 注意事項

- 元画像は自動的に削除・上書きされません。
- 変換後の画像には、元画像のすべての情報が保持されるとは限りません。
- Exif 情報には撮影場所などの個人情報が含まれる場合があります。
